const scale = 20

const floatTolerance = 0.000001

const axesSettings = {
    'stripLength': 10,
    'arrowLength': 20,
    'labelsFont': '20px Arial',
    'labelsFillStyle': 'red',
    'labelsMargin': 20,
    'strokeStyle': 'black',
    'lineWidth': 1,
}

const gridSettings = {
    'strokeStyle': 'black',
    'lineWidth': 0.15,
}

const supportPointSettings = {
    'radius': 7,
    'strokeStyle': 'black',
    'lineWidth': 1,
    'fillStyle': 'black',
}

const controlPointSettings = {
    'radius': 7,
    'strokeStyle': 'black',
    'lineWidth': 1,
    'fillStyle': 'red',
}

const curveSettings = {
    'strokeStyle': 'black',
    'lineWidth': 2,
}

export { scale, floatTolerance, axesSettings, gridSettings, supportPointSettings, controlPointSettings, curveSettings }